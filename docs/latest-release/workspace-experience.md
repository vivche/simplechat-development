---
layout: latest-release-feature
title: "Workspace Experience"
description: "How document cards, workspace branding, approvals, and shortcuts improve shared workspace navigation"
section: "Latest Release"
---

Workspace Experience updates make personal, group, and public workspaces easier to scan, brand, and govern.

## User Side

Workspace users can switch between list, cards, folders, and folders-plus-cards views, scan badges and document actions, chat with individual documents, and use branding cues to confirm the active workspace.

## Admin Side

Admins control which workspace types and document features are enabled, while group and public workspace owners configure shared branding such as hero colors and logos. The screenshots show both user browsing views and owner-managed branding controls.

## Custom Workspace Hero Colors

Group and public workspace owners can now choose from preset hero colors or use the custom color swatch on the manage page. The selected color updates the hero preview before saving, making it easier to confirm shared workspace branding.

<img src="{{ '/images/feature-group-custom-hero-color.png' | relative_url }}" alt="Annotated group workspace manage page showing a custom hero color swatch and live hero preview." style="width: 80%;" />

<img src="{{ '/images/feature-public-custom-hero-color.png' | relative_url }}" alt="Annotated public workspace manage page showing a custom hero color swatch and live hero preview." style="width: 80%;" />

## Why It Matters

Document-heavy spaces need fast scanning, clear ownership context, and predictable approval workflows when content moves between teams.

## How to Try It

1. Open a workspace document list and switch between list, card, folder, and folder-card views.
2. Review group and public workspace hero areas to confirm the active workspace context.
3. Use manage pages to configure colors, logos, and workspace branding when you own the workspace.
4. Watch for approval notifications when shared files or generated artifacts need review.

## Notes

- Available views can vary by workspace type and enabled settings.
- Branding helps users confirm which shared workspace they are working in.
- Custom hero colors are stored as normalized `#RRGGBB` values and use the same manage-page save flow as other workspace details.
- Approval flows keep shared and public knowledge from becoming searchable before review.
